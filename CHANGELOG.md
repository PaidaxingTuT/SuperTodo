# 更新记录 · CHANGELOG

## v2.8（2026-09-02）— 软件信息与在线部署
- 汉堡菜单「设置」右侧新增「**软件信息**」按钮（圆圈 ⓘ 图标，无文字）
- 关于弹窗：应用 logo、名称「清单」、版本 v2.8、作者「派大星TuT」
- **检查更新**：从 GitHub release API 拉取最新版本并对比提示（含新版本跳转下载）
- 开源仓库地址跳转 github.com/PaidaxingTuT/todo
- 新增 GitHub Actions：push `main` 自动部署 **GitHub Pages**，推送 `v*` 标签自动打包发布 **Release**

## v2.7（2026-09-02）— 配色柔和化
- 整体配色柔和淡雅：页面底色改淡蓝灰，顶部带主色系光晕氛围，不再单调
- 顶栏改为主色渐变（主色 → 深一档），并加柔和投影
- 新增主色派生变量（primary-soft / faint / deep），切换主题色时界面整体统一跟随
- 分组切换、计数、区块查看全部等容器底改为主色淡 tint；卡片阴影柔化

## v2.6（2026-09-02）— AI 改为云端增强开关
- **移除本地规则解析引擎**，一句话速记与智能整理统一走云端 AI（OpenAI 兼容）
- 设置新增「**启用 AI 增强**」开关：开启后即可填写 base / key / model 并启用速记与整理，关闭时输入框禁用
- **未开启时不显示一句话速记按钮**（右下角仅保留新增）
- 智能整理需开启后使用；解析过程显示加载态，云端失败时给出明确提示

## v2.5（2026-09-02）— 导航精简与自适应打磨
- 汉堡栏底部三个按钮合并为一个「设置」入口
- **移除底部操作栏**，右下角改为两个常驻浮动按钮：一句话速记（上）+ 新增（下），尺寸统一 54px
- 排序按钮移至**列表页右上角**（首页隐藏），选中非默认排序时高亮
- 「智能整理」入口并入设置弹窗（数据区块）
- **修复**：宽屏下搜索框只占左半（多余 flex 占位撑开）；现输入框随视口铺满
- **自适应**：AI 速记弹窗加宽（桌面 560px / 手机 92vw），弹窗内文字间距加大；修复表单字段 label 高度塌陷，新增表单字段间距加大
- **修复**：设置图标改回小齿轮（原为购物车图标）
- **修复**：新建表单中类型 / 场景 / 时间标签的「＋」不生效（点击被普通标签分支拦截）；现添加后表单即时刷新并自动选中新标签，加号符号垂直居中
- **优化**：主页二级分组方式（按场景 / 按时间）从汉堡菜单移至首页顶部固定显示，切换即时生效
- **优化**：设置弹窗内按钮间距加大

## v2.4（2026-09-02）— 第四次增强：AI 一句话速记 + 智能整理
- 新增 **AI 一句话速记**：底部 ✨ 按钮输入一句话，自动解析类型 / 场景 / 时间 / 花费 / 截止日期 / 重要度，填好表单后确认保存
- **本地规则引擎**：内置词表 + 动态匹配现有自定义标签，完全离线可用
- **云端增强（可选）**：设置面板可配 OpenAI 兼容接口（base URL / key / 模型），有 key 自动走云端，失败或无 key 自动回退本地规则
- **建议式新建标签**：解析到不存在的标签时，表单提示「建议新建『XX』」，默认勾选可取消，保存时自动建标签并归类
- 新增 **智能整理**：汉堡菜单入口，扫描未分类 / 未分组事项，列表预览 + 建议值预选 + 一键采纳
- **补全分组内手动拖拽排序**：列表页未完成项显示拖动把手（≡），仅「默认」排序下可拖，落位后持久化；完成项沉底不受影响
- **修复 Bug**：空状态底部栏被隐藏导致无法添加 / 速记；现空状态保留 ＋ 和 ✨ 入口（排序 / 计数隐藏）

## v2.3（2026-09-02）— 第三次增强：全面自定义
- 新增**配色主题**：预设 10 色 + 自定义取色器，整界面跟随变色并持久保存
- 底部栏最右侧新增 **⚙️ 设置**按钮
- 设置面板重构为分段式：外观·主色 / 一级·类型 / 二级·场景 / 二级·时间 / 数据
- 汉堡菜单里的类型：**长按 → 重命名 / 删除**，底部新增"＋新增类型"
- 类型 / 场景 / 时间标签可统一管理（设置面板内点名字重命名、✕ 删除、＋ 添加）
- **修复 Bug**：主页区块内点击预览事项，误进入分组视图而非编辑；现修正为点击预览事项直接弹出编辑

## v2.0（2026-09-02）— 重构为汉堡 + 分区架构
- 推翻 v1 的"横向 chips 筛选"，改为：
  - 一级标题（类型）→ 侧边汉堡抽屉
  - 二级标题（分组）→ 主页大区块，可按场景 / 时间切换分组
  - 三级 → 点进区块的详情列表
- 修复顶部重叠、添加弹窗超出屏幕、添加无法使用等关键问题

## v1.0（2026-09-02）— 首版
- 快速添加 + 完整表单
- 多类型（购物/待办/计划/旅游/愿望）、多标签交叉分类
- 排序（花费/重要度/截止日期/创建时间/默认） + 手动拖动
- 完成/未完成、备注、★重要度、预估花费、截止日期、搜索
- 本地存储 + 导出/导入备份
- PWA（manifest + service worker）

---
### 开发期注意事项
- Service Worker（`sw.js`）在开发期**保持禁用**（避免旧缓存问题），打包 APK / 正式发布时**必须启用**做离线缓存。
