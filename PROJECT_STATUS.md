# 项目进度 · PROJECT STATUS

> 更新：2026-09-02

## 一、目前完成到哪一步

**功能层面：全部核心需求已实现并通过测试。**

| 需求 | 状态 |
|------|------|
| 快速记录 + 完整表单添加 | ✅ |
| 多类型统一管理（购物/待办/计划/旅游/愿望） | ✅ |
| 灵活分类：类型 / 场景 / 时间 多标签交叉 | ✅ |
| 排序：花费 / 重要度 / 截止 / 创建 / 默认 | ✅ |
| 标记完成 / 未完成 | ✅ |
| 备注 / 预估花费 / 截止日期 / ★重要度 | ✅ |
| 本地存储 | ✅ |
| 导出 / 导入 / 清空备份 | ✅ |
| 手动拖动排序 | ⚠️ 见下方说明 |
| **三级架构：汉堡(一级) → 分组(二级) → 事项(三级)** | ✅ |
| **全面自定义：类型/场景/时间 增删改** | ✅ |
| **自定义配色主题** | ✅ |
| **设置入口（汉堡菜单，导航精简）** | ✅ |
| **AI 一句话速记（云端）** | ✅ |
| **智能整理（批量补标签）** | ✅ |
| **AI 增强开关（OpenAI 兼容）** | ✅ |
| **分组内手动拖拽排序** | ✅ |

## 二、当前已知 Bug 与需改点

### 已修复（本轮）
- 主页区块内点击**预览事项**，之前会误进分组视图，现改为直接弹出编辑 ✅
- 空状态底部栏被隐藏导致无法添加 / 速记，现保留 ＋ 和 ✨ 入口 ✅

### 仍在评估 / 待确认
1. **跨分组移动**（把事项从一组拖到另一组，会改写场景 / 时间字段）——本期未做，仅支持分组内拖动排序，如需可后续补
2. **PWA 离线缓存（sw.js）处于禁用状态**
   - 开发期避免旧缓存特意禁用；正式发布 / 打包 APK 前必须重新启用，否则"添加到主屏幕"后断网用不了。
3. **无自动化深度测试**
   - 原生 JS 手写，交互逻辑靠浏览器脚本验证，尚未做自动化回归测试。

## 三、下一步

按优先级：

### 近期（推荐先做）
1. **启用 Service Worker**（HTML 网络优先 + 静态资源缓存优先），做离线缓存验证
2. **打包成独立 APK**（自签名）：先确认 web 端体验，再在沙盒装 Android SDK/Java，用 WebView 壳打包
3. 若需要**手机 / 电脑同步**：做数据文件同步方案（本期暂不做，沿用导出/导入备份）

### 可选增强（按需）
- 背景深浅模式（深色 / 浅色）
- 字体大小 / 列表密度调节
- 首页分组更多维度的筛选
- 更多图标 / 标签颜色系统

---

### 打包 APK 的路线图
1. 沙盒安装 Java + Android SDK（build-tools）
2. 生成 WebView 壳工程（MainActivity 加载本地 `index.html`，禁用联网缓存异常）
3. 把本项目文件放进 APK assets
4. 自签名生成 `清单.apk`
5. 手机侧允许"安装未知来源应用"后安装
